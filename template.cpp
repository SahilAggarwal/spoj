#include <iostream>
#include <stdio.h>
#include <stdlib.h>

using namespace std;

int main() {
        int t, temp, n1, n2;
        scanf("%d", &t);
        temp = t;
        while(temp--) {
                scanf("%d %d", &n1, n2);
        }
}
